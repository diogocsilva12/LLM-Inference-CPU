# Vanilla llama.cpp workspace

This folder is reserved for a clean upstream clone of `ggml-org/llama.cpp` with no TurboQuant fork changes.

Default clone target:
- `llamacpp-vanilla/llama.cpp`

Use these scripts from the project root:
- `slurm/clone-vanilla-llama.sh`
- `slurm/build-vanilla-llama.sh`
